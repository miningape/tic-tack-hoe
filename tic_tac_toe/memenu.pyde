import tic_tac_toe

class menu:
    def __init__():
        print("Running")
    
    def show(leng):
        line(10, 10, leng, 10)
        
    def input():
        flag = True
        if (keyPressed == True):
            if (key == 'c'):
                flag = False
                delay(20)
        return flag
